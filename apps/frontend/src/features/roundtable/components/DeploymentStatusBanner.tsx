export function DeploymentStatusBanner() {
  return <div className="rounded-2xl border border-blue-100 bg-blue-50 p-4 text-sm text-blue-800">AI SDK Text Stream + FastAPI + Render Postgres 首版协议：流完成后自动 refetch session 恢复 transcript/artifacts。</div>
}
