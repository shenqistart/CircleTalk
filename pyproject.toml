[project]
name = "bedrock-workspace"
version = "0.1.0"
requires-python = ">=3.13"

[tool.uv.workspace]
members = ["packages/*", "apps/backend"]

[tool.uv.sources]
bedrock-core = { workspace = true }
bedrock-llm = { workspace = true }
bedrock-knowledge = { workspace = true }
